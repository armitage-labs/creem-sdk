# CancelSubscriptionRequestEntityMode

The mode of cancellation (immediate or scheduled), default can be configured in the store billing settings.

## Example Usage

```typescript
import { CancelSubscriptionRequestEntityMode } from "creem/models/components";

let value: CancelSubscriptionRequestEntityMode = "immediate";
```

## Values

```typescript
"immediate" | "scheduled"
```